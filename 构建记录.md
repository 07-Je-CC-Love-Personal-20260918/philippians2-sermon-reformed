# 腓立比书 2:1-18 × 海德堡主日 24 讲章全解 · 构建记录

## 交付物
- **线上地址（Netlify）**：https://philippians2-sermon-reformed.netlify.app
- **线上地址（GitHub Pages）**：https://07-je-cc-love-personal-20260918.github.io/philippians2-sermon-reformed/
- **源码仓库**：https://github.com/07-Je-CC-Love-Personal-20260918/philippians2-sermon-reformed
- **Netlify 项目**：`philippians2-sermon-reformed`，site id `e40af333-5969-4581-b93d-bd1b79bc5c47`
  - deploy id `6aae5002762d7b5eacd714f6`（state=ready，published 2026-09-19，deploy_time 24s）
  - 管理后台 https://app.netlify.com/projects/philippians2-sermon-reformed
- **GitHub Pages**：source = `main` 分支 `/docs` 目录，status=built（`docs/index.html` 为 `site/index.html` 的副本，另有 `.nojekyll`）
- **本地源码**：`/home/claude/php2/src/*.py` → `cd src && python3 build.py` → `../site/index.html` + `../docx/content.json`
- **DOCX**：`docx/render.js`（docx@9.6.x）→ 约 2.2 MB，69 页 A4
- **HTML 体积**：约 240 KB，39,600 中文字符，纯静态，零外部资源（仅 4 条姊妹站超链接）

## 这一份与信经系列的不同
前四份（尼西亚／亚他那修／威斯敏斯特／多特）是**信条全解**；本份是**单次主日讲道的全解**，
体裁是欧陆改革宗讲章（引言—分点—四重应用—结语），外面再包一层研经与教理的支架。
因此结构上多了两块信经系列没有的东西：**第五部完整讲章正文**、**第十部五道讨论题的逐题答案分析**。

## 内容结构（十三部 + 附录，14 个 section，81 个目录锚点）
0. **序** — 三条线索（救赎历史／经文／要理问答）；两种致命误引（特伦特正典 32 条 vs 廉价恩典）
1. **全景** — 神做工/人做工的四种排列表；救赎历史七幕坐标；神论→基督论→救恩论→伦理学的次序；
   为何连「部分的义」都不存在；三项合一信条 + 威敏对本段的处理表
2. **情境** — 腓立比城身份档案（腓力二世金矿／主前 42 年战役／ius Italicum／无会堂）；
   徒 16 三个人（吕底亚·使女·禁卒）；写作地点三说表；四重压力表；
   1563 海德堡来历；**特伦特第六期 vs 海德堡主日 24 对照表**
3. **脉络** — 段落真正起点在 1:27（[[πολιτεύεσθε]]）；1:27-2:18 同心圆 ABCD-C′B′A′；
   四个关键连接词表；基督颂是不是引用的早期诗歌
4. **逐节** — 2:1-18 切成九个语义单元，每单元原文要点/释义/神学/落点；
   [[ἡγέομαι]] 三次出现一条线（2:3 / 2:6 / 3:7-8）
5. **讲章正文** — 引言（两种人同时不舒服）· 先解总纲「作成≠赚取」·
   第一要点「因神在你心里运行」（是谁/到什么程度/什么方式/所以战兢）·
   第二要点「为要成就他的美意」（εὐδοκία 用法表/三样内容/保证/喜乐）·
   四重应用（教训 doctrina·责备 redargutio·安慰 consolatio·劝勉 exhortatio）· 结语（V 字形）
6. **字义** — κατεργάζομαι 用法表（含**罗 7:18 是关键对照**）、ἐνεργέω 分布表、
   εὐδοκία 两种读法、「恐惧战兢」全圣经用法表
7. **主日 24 逐问** — 问 62 三段论表 + 善行三重标准（问 91）反用；
   问 63 工价 vs 赏赐表；问 64「不可能不」是本性判断；同一反对的四种回答对照表
8. **跨经专题** — 主权与责任五方案表；称义/成圣六项区分；保罗与雅各；三重归算；
   确据四文献表；**十个常见误读表**
9. **拓展** — 2:19-30 两个活标本（以巴弗提「几乎至死」与 2:8 同词组 μέχρι θανάτου）；
   3:4-14 保罗自己的虚己（与 2:6-11 五步平行）；4:2-3 落地点；
   虚己论三种理解表 + extra Calvinisticum；与三大信经接点；**平行经文十处表**
10. **讨论题详解** — 用户给的五题，每题「题意辨析→分层答案→常见偏差表→一句话总结→若只够问一句」
11. **三重闭环** — ①救赎历史·圣约（十环表 + 圆环图，合拢点＝同一个「美意」）
    ②经文·圣经神学（八处旧约回响表 + 时间轴图，合拢点＝跪下）
    ③罪-救赎-感恩（腓 2:12-13 一节含三段 + 三角图）+ 三环合一总表
12. **应用与操练** — 五种处境表；一周操练表；三个记忆钩子；四段背诵卡
附：腓 2:1-18 全文、主日 24 全文、30 条术语表、23 条信条索引、延伸阅读四步、全文结构一览

## 核心神学定位（复用时注意）
- 全篇以**那个「因为」（γάρ）**为轴：神的运行是人努力的**根据**，不是让步、不是对手
- 「作成」κατεργάζομαι = 把已领受的推进到完成，**从不表示赚取**；且是**现在式命令 + 复数**，
  首要指会众合一（2:1-4；4:2），其次才是个人灵修
- 「恐惧战兢」= 儿子之敬畏（timor filialis），非奴仆之惧；证据是 13 节的「因为」+ 1:6 + 弗 6:5 的用法
- 「虚己」= **以加上的方式倒空**（取了奴仆的形像），坚决排除十九世纪虚己论；配 extra Calvinisticum
- 善行：**从地基搬到果子**——问 62、63 拆地基，问 64 立刻立果子，两边同时讲，不可只拆一边
- 对天主教论点讲**准**不讲**漫画**：明写特伦特也否定人能自行开始归向神，分歧点精确定位在
  「宣告 vs 注入」与「善行是否 vere mereri」
- 世人的善行给公道评价：多特三·四 4「本性之光的余辉」、公民之义是真实的善，只是不能作义
- 主日 24 的**位置**本身是论证：它在「救恩」篇末尾而非「感恩」篇，先拆错误的地基，再由问 86 正面立

## 技术要点
- 沿用本项目既有策略：**系统中文字体栈，不引用 Google Fonts**（珠海，fonts.googleapis.com 不可达）
- 一份内容两种渲染：`dsl.py` 定义区块 DSL（p/lead/h3/h4/ul/ol/table/scr/cat/app/qa/fig/hr），
  `build.py` 出 HTML，同时 dump `content.json` 给 `docx/render.js`
- 行内标记：`**粗**` `{{关键词}}` `[[希腊文]]` `((出处小字))`；
  **注意**：HTML 转义在标记解析之前，所以出处标记不能用 `<<>>`（会被转成实体），改用 `(())`
- 13 幅手写 SVG，CSS 变量着色、双主题自适应；`style="min-width:..."` 让窄屏在图框内横向滚动而非压扁
- 目录高亮：**rAF + getBoundingClientRect**（与既有记录一致，勿用 IntersectionObserver）
- DOCX 图片走 **Playwright 截图**（`shots.py`，deviceScaleFactor=2 → 1920px 宽 PNG）而非 SVG 转换器，
  沙箱没有 cairosvg/rsvg，但有 Noto CJK 字体，截图路线中文渲染正确且与网页完全一致

## 本次新踩的三个坑（重要）
1. **`scroll-padding-top` 与 `scroll-margin-top` 会叠加。**
   html 上设了 `scroll-padding-top:74px`，又在 `h3`/`section.part` 上设 `scroll-margin-top:74px`，
   `scrollIntoView` 后目标停在 **148px**，高于高亮判定线 90px，于是目录高亮**整体偏前一格**，
   Playwright 实测 1/17 命中。删掉 `scroll-margin-top` 后 17/17 全中。
   **这是真 bug，不是测试环境问题**——手动滚动时用户也会看到高亮慢一格。
2. **docx@9 的 `spacing.line` 不给 `lineRule` 时，LibreOffice 按「固定行高」处理**，
   导致所有 `ImageRun` 被压成一条约 6px 的细线（Word 里可能正常，但导出 PDF 就废了）。
   必须在**每一处**设了 `line` 的地方补 `lineRule: LineRuleType.AUTO`，包括 `styles.default.document.paragraph`。
   修好后页数从 55 → 69，图片全部正常。
3. **同一张 SVG 在页面里复用多次 → 内部 `marker` 的 `id` 重复。**
   `FIG_GAR` 用了三次，`<marker id="ar4">` 就出现三次，HTML 出现重复 ID。
   解法：`dsl.prep_svg()` 在插入时给所有内部 id 加 `-f{n}` 后缀，并同步改写 `url(#...)`。
   同一函数顺便把 SVG 里的 `[[...]]` 标记剥掉（SVG 不经过 `inline()`）。

## 验证结果
- 静态：81/81 锚点有效、95 个 ID 无重复、34 类标签全平衡、零外部资源、四类行内标记零残留、
  SVG `<text>` 内无 `<b>`；计数 14 section / 67 个 h3 / 62 表 / 13 图 / 17 组问答 / 21 经文框 / 10 要理框 / 14 应用框
- 浏览器（http://127.0.0.1:8901 实测）：
  - 横向溢出 320/360/390/414/480/560/768/820/1024/1280/1440/1920 共 **12 档全部 0 溢出、0 JS 错误**
  - 目录滚动高亮 **17/17 命中**（测试须先注入 `html{scroll-behavior:auto}` 并用 `behavior:'instant'`，
    否则又会被平滑滚动坑成假阳性——这是第三次踩同一个坑了）
  - 交互：主题三态循环、展开/收起全部问答 17/17、阅读进度条 100%、移动端抽屉开启与 Esc 关闭、打印隐藏侧栏
  - **13 幅图文字零出框**（修了两处：FIG_ARC 的 ③ 改 `text-anchor="end"`；FIG_COV 第 ④ 框文字缩短）
- DOCX：OOXML validator **All validations PASSED**（2216 段落）；LibreOffice 转 PDF 69 页，
  封面/目录/表格/图片/标注框逐页抽查正常
- 线上：Netlify deploy state=ready、published_at 与 screenshot_url 均存在；
  GitHub Pages API status=built；`raw.githubusercontent.com` 取回的 `docs/index.html` 与本地构建产物**逐字节一致**

## 与既有记录的差异
- 上几份记录说「线上校验走 WebFetch，不要用 curl」。**本次 WebFetch 也不可用**：
  Netlify／GitHub Pages 的 URL 未出现在任何搜索结果里，WebFetch 以 PERMISSIONS_ERROR 拒绝；
  curl 则被出口代理挡下（`*.netlify.app` 与 `*.github.io` 均返回 403）。
  **本次采用的替代校验：① Netlify `get-deploy-for-site` 读 state/published_at/screenshot_url；
  ② `raw.githubusercontent.com`（在白名单内）取回 Pages 源文件做逐字节比对；③ GitHub Pages API 读 status。**
  下次同类任务直接走这三条，不要在 curl / WebFetch 上耗时间。

## 后续修改流程
```bash
cd /home/claude/php2/src && python3 build.py && python3 validate.py
(cd ../site && setsid python3 -m http.server 8901 &) ; sleep 2
python3 browser_test.py && python3 shots.py          # 改了图才需要重跑 shots
cd ../docx && node render.js
cp ../site/index.html ../docs/index.html
cd ../site && npx -y @netlify/mcp@latest --site-id e40af333-... --proxy-path "..."   # 同一 site id，URL 不变
cd .. && git add -A && git commit -m "..." && git push                                # Pages 自动重建
```

## 姊妹站（本系列）
- 尼西亚信经：https://nicene-creed-reformed.netlify.app
- 亚他那修信经：https://athanasian-creed-reformed.netlify.app
- 多特信经：https://canons-of-dort-reformed.netlify.app
- 威斯敏斯特信条：https://westminster-confession-reformed.netlify.app
